server:  port: 8080  shutdown: gracefulmanagement:  endpoint:    health:      probes:        enabled: true  server:    port: 8081spring:  profiles:    active: '@profileActive@'  webflux:    base-path: /questionnaire  lifecycle:    timeout-per-shutdown-phase: 20s  jackson:    date-format: yyyy-MM-dd HH:mm:ss    time-zone: GMT+8  redis:    host: localhost    port: 6379    username:    password:  r2dbc:    url: r2dbc:mysql://localhost:3306/test_survey?characterEncoding=UTF-8&useSSL=false&sslMode=DISABLED    username: root    password: fshuai123    pool:      validation-query: SELECT 1      enabled: true